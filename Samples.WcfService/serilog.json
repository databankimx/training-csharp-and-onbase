{
  "//": "*Migration Note: Serilog is DataBank's current logging standard, replacing log4net. This file defines WHERE and in WHAT FORMAT logs are written, so that can change without a recompile. It deliberately does NOT define \"MinimumLevel\", that's still driven at runtime by ExampleWebService.svc.cs from Web.config's existing <serviceSettings debugMode=\"...\"> setting, via a Serilog.Core.LoggingLevelSwitch, so debug logging can still be toggled per-deployment through Web.config alone, exactly like Samples.AsmxWebService. See LectureNotes.md.",
  "Serilog": {
    "WriteTo": [
      {
        "Name": "File",
        "Args": {
          "path": "C:\\Temp\\Logs\\WcfExampleWebService.log",
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff} - [{Level}] {Message}{NewLine}{Exception}{NewLine}"
        }
      }
    ]
  }
}
